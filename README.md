RadarKeys — an external InfiniteHeaven module

Allows keybinds to run scripts through button interactions using the IHHook implementation.
It runs on a separate thread, running side by side with other hooks and frameworks.

**Installation**
1. Install InfiniteHeaven
2. Place the RadarKeys.dll in the main directory (with the mgsvtpp.exe).
3. Place the RadarKeys_Core.lua in MGSV-TPP directory → ".../mod/modules" 
4. (Optional) Create a new folder named "radarKeys" inside the "mod" folder (not in the "modules" folder!)
5. Default hotkey is F7 to open the RadarKeys menu in-game.

Usage:
1.  WIP

**Changelogs:**
- [8/26/2026]
-   First successful build. Working condition. Probably ready for release, but fixing some UI related concerns before it.
